package main

// Bulk page authentication is handled in the browser using the existing admin session token.
// The route itself is intentionally public so normal navigation can load the page; all data/import APIs remain protected.
